# Optional. If set, the /api/revalidate route requires this secret.
# Vercel Cron automatically sends it as `Authorization: Bearer <CRON_SECRET>`.
# You can also call the route manually with ?secret=<CRON_SECRET>.
CRON_SECRET=
